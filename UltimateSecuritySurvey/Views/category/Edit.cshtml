@model UltimateSecuritySurvey.Models.QuestionCategory

@{
    ViewBag.Title = "Edit category";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>QuestionCategory</legend>

        <div class="editor-label">
          <strong>  @Html.LabelFor(model => model.categoryId) </strong>
        </div>
        <div class="editor-field">
            @Html.DisplayFor(model => model.categoryId)
            @Html.ValidationMessageFor(model => model.categoryId)
        </div>

        <div class="editor-label">
        <strong>    @Html.LabelFor(model => model.categoryName) </strong>
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.categoryName)
            @Html.ValidationMessageFor(model => model.categoryName)
        </div>

        <div class="editor-label">
        <strong>    @Html.LabelFor(model => model.description) </strong>
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.description)
            @Html.ValidationMessageFor(model => model.description)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
